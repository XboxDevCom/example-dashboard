<Page
    x:Class="Dashboard.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dashboard.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    UseSystemFocusVisuals="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <ScrollViewer>
            <StackPanel Spacing="24">
                <TextBlock Text="Start"
                           FontSize="32"
                           FontWeight="Bold"/>
                <TextBlock Text="Willkommen im Dashboard! Hier finden Sie einen schnellen Überblick."
                           FontSize="18"
                           TextWrapping="Wrap"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,0,12,0">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Schnellzugriff" FontSize="20" FontWeight="SemiBold"/>
                            <Button Content="Musik" MinHeight="48" HorizontalAlignment="Stretch"/>
                            <Button Content="Spiele" MinHeight="48" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="6,0,6,0">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Letzte Aktivität" FontSize="20" FontWeight="SemiBold"/>
                            <ListView>
                                <ListViewItem Content="Musik-Player geöffnet" MinHeight="48"/>
                                <ListViewItem Content="Spiel gespeichert" MinHeight="48"/>
                                <ListViewItem Content="Einstellungen geändert" MinHeight="48"/>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="12,0,0,0">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Tipp des Tages" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock TextWrapping="Wrap"
                                       Text="Verwenden Sie die Menütaste auf dem Gamepad, um schnell zwischen den Bereichen zu wechseln."/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
