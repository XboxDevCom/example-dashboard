<Page
    x:Class="Dashboard.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dashboard.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    UseSystemFocusVisuals="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="20"
                        MaxWidth="500">
                <Border Width="120"
                        Height="120"
                        Background="{ThemeResource SystemAccentColor}"
                        CornerRadius="12"
                        HorizontalAlignment="Center"/>

                <TextBlock Text="Dashboard"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Eine Beispiel-Anwendung für die Xbox One, die NavigationView-Navigation demonstriert."
                           FontSize="16"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>

                <Button Content="Lizenz anzeigen"
                        MinHeight="48"
                        HorizontalAlignment="Center"
                        Click="ShowLicense_Click"/>

                <TextBlock Text="Entwickelt von XboxDev.com Team"
                           FontSize="14"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Copyright © 2018 XboxDev"
                           FontSize="14"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
